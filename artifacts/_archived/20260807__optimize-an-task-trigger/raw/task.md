# 任务背景

一次真实异常：对话连续讨论了 an-task 的触发机制（设计讨论阶段），最后用户说"可以，开始吧"，
AI 误以为只是"按刚才讨论的方案实现"，跳过了 an-task 的分流流程，直接执行。

根因：`an-task` 的发现描述只写"适用于需要 AI 执行的任务"，没有定义"从讨论态切换到执行态"的
触发判定，导致模型把连续语境中的"开始吧"当成普通继续。

用户要求：优化 an-task 的触发机制，使"一旦用户明确下达开始/实现指令，即使前面讨论再多，也要先过 an-task 分流"成为可自动判定的规则。
