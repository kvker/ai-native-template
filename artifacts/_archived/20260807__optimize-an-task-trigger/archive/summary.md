# 完结摘要

## 最终产出

an-task 触发机制已完成优化：

- an-task 发现描述新增进入执行态的触发信号（开始吧、开始做、实现、动手、开工、按方案执行），隐式调用也能命中。
- an-task 正文新增“触发与逐消息重新分流”章节：讨论态不触发；明确执行指令必须进入分流；每条新消息重新分流；无法判断时按 flow-policy 确认。
- openai.yaml 元数据与初始化生成的根 AGENTS 路由要求同步包含触发规则。
- 契约测试锁定“连续讨论后下达开始/实现指令必须进入 an-task 分流”的异常场景；61 项测试全部通过。
