# 完成标准

## 目标

修复归档事务脚本的两处缺陷，使其在错误路径输出真实信息、首次归档无需手工建目录。

## 完成标准

- [ ] AC-1: archive-task.mjs 的 CLI 入口在参数错误或门禁失败时输出真实错误信息与正确退出码，不再出现 TDZ 异常。
- [ ] AC-2: resolvePaths 在 `_archived` 目录不存在时自动创建，首次归档无需手工建目录。
- [ ] AC-3: 新增回归测试覆盖上述两个缺陷场景，全部测试通过。
