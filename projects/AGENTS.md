# projects 目录说明

`projects/` 是实际工作区和交付物根目录。每个子目录代表一个独立的工作单元，可以是代码工程、文档集、数据集、设计稿目录或任何需要被 AI 理解和操作的交付物集合。

## AI 行为

- 每个子目录视为独立项目，按需读取其内部结构和文件。
- 实际交付物写入此目录，而非 `artifacts/`。
- 新建项目时创建独立子目录，并在根 `AGENTS.md` 中登记。
