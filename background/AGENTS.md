# background 目录说明

`background/` 存放从工作区材料和用户输入中确认的稳定背景知识，包括工作区定位、主要材料、工作单元关系、长期事实和可复用能力。

## AI 行为

- 需要理解工作区背景时，按需读取相关文档。
- 除 `/an-init`、`/an-refresh` 或用户明确要求外，不主动修改本目录。
- 更新时保持增量，不重写人工补充内容。
- 从工作区材料反推的信息要标注来源；无法确认的信息标记为“待确认”。
- 背景文档与 `projects/` 工作区内容冲突时，先以材料事实为准，并在更新摘要中说明冲突。
