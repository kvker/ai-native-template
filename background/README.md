# 背景知识库

此目录存放项目特定的稳定背景知识，供 Codex 按需读取。

## 约束

- `$an-init` 执行期间允许 Codex 生成和更新此目录下的文档。
- 初始化完成后，本目录默认视为稳定背景，Codex 不应主动修改。
- 当业务、技术栈或架构事实发生变化时，应通过 `$an-refresh` 增量刷新。
- 背景文档与 `target/` 实际代码冲突时，以代码事实为准，并在刷新时记录差异。
